<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.izhang.filmfriend">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.Loading" />
        <activity android:name=".View.RegisterView" />
        <activity
            android:name=".LoginView"
            android:label="@string/title_activity_login_view" />
        <activity
            android:name=".View.HomeView"
            android:label="@string/title_activity_home_view" />
        <activity android:name=".View.MovieDetailView" />
        <activity android:name=".View.GroupDetailView"></activity>
    </application>

</manifest>